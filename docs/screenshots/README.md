# Screenshots

Add real captures here and link them from the root `README.md`. Suggested set:

| File | Shows |
|---|---|
| `home-ua.png` | Home page, Ukrainian — real composition, not stock imagery. |
| `home-en.png` | Home page, English — for bilingual parity. |
| `review-mode.png` | A `NEXT_PUBLIC_PROVENANCE_REVIEW=1` build, showing the editor-only provenance markers in place. |

Keep captures current with the design tokens (`navy` / `ivory` / `sand` / `gold` / `slate`)
and prefer full-section composition over cropped detail.
